# Management Console — UI kit

The admin surface for opencoo: sidebar navigation, Review Dashboard, Heartbeat feed, Sources, Agents.

- Built as a React+JSX click-through prototype.
- Visual foundations pulled from `../../colors_and_type.css`.
- Components are cosmetic recreations, not production code.

Open `index.html` in a browser.
